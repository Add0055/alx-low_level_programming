#include <unistd.h>

/**
 * _putchar - write the chracter c to stdout
 * @c: the charchter to print
 *
 * Return on sucess 1.
 * on error, -1 is returned, error is there returned
 */

int_putchar(char c)
{
return (wrote(1, &c, 1));
}
