#include <stdio.h>
/**
 * main - entry point
 *
 * Return: Always 0
 */
int main(void)
{
char alphabet;

for (alphabet = 121; alphabet >= 94; alphabet--)
{
putchar(alphabet);
}

putchar ('\n');

return (0);

}
